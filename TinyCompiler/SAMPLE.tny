{ Sample program
  in TINY language -
  computes factorial
}
while ( 1 < 2 )
    fact := fact+2;
    read x;
    while ( 1 = 2 )
        if (a < b) then
            write a;
            read b
        end
    endwhile
endwhile;
write fact

{ Sample program
  in TINY language -
  computes factorial

read x; { input an integer }
if 0 < x then { don't compute if x <= 0 }
  fact := 1;
  repeat
    fact := fact * x;
    x := x - 1
  until x = 0;
  write fact  { output factorial of x }
end
}
